numpy~=2.4
scipy~=1.17
pandas~=2.3
matplotlib~=3.10
CoolProp~=7.2
reportlab~=4.5
pyinstaller~=6.20

# Alternative transient blowdown engine (optional but recommended).
# Note: HydDown 0.16.x multicomponent PH-solver is incompatible with numpy 2.x.
# The multicomponent HydDown smoke test is skipped on numpy >= 2.
hyddown==0.16.2

# Vendor catalog schema validation (see psv_vendor_catalog.py).
cerberus~=1.3
